DB_URL = "mongodb+srv://databaseexample.mongodb.net/"

DB_URL_TEST = "mongodb+srv:/databaseexample.mongodb.net/servicosPublicos_teste"

APP_PORT=5011

NODE_ENV=development

DEBUGLOG=true

# JWT (JSON Web Token) Configurações
JWT_SECRET_ACCESS_TOKEN=ajdjfkenlcdlf
JWT_SECRET_REFRESH_TOKEN=kjakjfjfekff
JWT_SECRET_PASSWORD_RECOVERY=senha_recuperacao_secret_key_32chars_min
JWT_ACCESS_TOKEN_EXPIRATION="2m"
JWT_REFRESH_TOKEN_EXPIRATION="3d"
JWT_PASSWORD_RECOVERY_EXPIRATION="1h"
# O JWT Refresh Token só é concedido, se não estiver vencido, no /login. Em /refresh, apenas se esse valor for true.
SINGLE_SESSION_REFRESH_TOKEN=false

EMAIL_HOST="example"
EMAIL_PORT=1111
EMAIL_USER="28728"
EMAIL_PASS="9898"
# false para TLS, true para SSL
EMAIL_SECURE=false
# Controla se o envio de e-mails está ativo (true ou false)
DISABLED_EMAIL=true

# Email do administrador para receber notificações de erro
ADMIN_EMAIL="example@example.com"  # Obs.: já declarado na seção de Administrador

# URL do Frontend e Base para links enviados por email
# IMPORTANTE: Em produção, altere para o domínio público real
# Exemplo: https://servicospublicos.vilhena.ro.gov.br
# Esta URL é usada nos emails de recuperação de senha e boas-vindas
FRONTEND_URL="http://localhost:3000"
BASE_URL="http://localhost:5011"

# Configurações do Mailsender
URL_MAIL_SERVICE="http://mailsender-servicos:5016/api/emails/send"
MAIL_API_KEY="AUTO_GENERATED_ON_STARTUP"  # Gerada automaticamente pelo script init-mailsender.sh

# Configurações do E-mail (OBRIGATÓRIO - Substitua com suas credenciais reais)
# Para gerar senha de aplicativo do Gmail:
# 1. Acesse: https://myaccount.google.com/apppasswords
# 2. Crie uma nova senha de app
# 3. Cole abaixo (remova os espaços)
SENDER_EMAIL="seu-email@gmail.com"
SENDER_PASSWORD="xxxx xxxx xxxx xxxx"

# Chave Mestra do Mailsender (use uma chave forte)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_KEY="gere-uma-chave-forte-de-64-caracteres-hexadecimais-aqui"

# Configurações do MinIO (S3-compatible storage)
# Endpoint do servidor MinIO (sem http/https)
MINIO_ENDPOINT=localhost
# Porta do MinIO
MINIO_PORT=9000
# Usar SSL (true/false)
MINIO_USE_SSL=false
# Chave de acesso do MinIO
MINIO_ACCESS_KEY=sua-chave-de-acesso
# Chave secreta do MinIO
MINIO_SECRET_KEY=sua-chave-secreta
# Nome do bucket para armazenar fotos
MINIO_BUCKET_FOTOS=seu-bucket-de-fotos